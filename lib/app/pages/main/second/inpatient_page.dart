import 'package:flutter/material.dart';

class InpatientPage extends StatelessWidget {
  const InpatientPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
