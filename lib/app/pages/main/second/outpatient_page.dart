import 'package:flutter/material.dart';

class OutPatientPage extends StatelessWidget {
  const OutPatientPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
